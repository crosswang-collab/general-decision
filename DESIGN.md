# 決斷連 · 軍用口令簿

視覺提案 v1 · 2026-09-13 · 390 × 844

以 crosswang-collab/general-decision 的 main 與 decide-mock-v5.html 為基準。這一版改變視覺層級、角色的出場方式與操作回饋；七張情境卡、三位班長、API 與本機資料架構沿用原專案。正式角色仍使用原有 Avatar 介面與 `public/officers/{level}/{mood}.webp` 路徑。

## 使用交付物

- `index.html`：畫面總覽，可切深淺色，並開啟可操作預覽。
- `play.html`：可獨立開啟的操作示範，使用固定展示資料，不會呼叫真實下令／週報 API。
- `screens/light`、`screens/dark`：七畫面、日記空狀態、啟動畫面，各為 390×844 PNG。
- `app-icon-1024.png`：1024×1024、不透明、無文字的帽徽圖示；深淺色共用同一識別圖案。
- `general-decision-source.zip`：完整原始碼交付，不需手動合併片段。
- `CHECK.md`：獨立檢查與執行紀錄；未把受環境阻擋的測試寫成通過。

## 視覺決策

紙色保持卡其；深橄欖綠承擔「按這裡」與命令的重量。金色用在軍階、圖示與完成強調，不作大面積背景裝飾。首頁「吃」跨三欄，以 80px 主字形成第一眼焦點；「去／赴／歇」各佔一欄，維持四個入口。沒有增加額外主按鈕。

命令卡移除黑色描字，以字級、字重、分隔線區分三段。角色在右欄中央、獨立圓形底上，與左欄文字彼此不重疊。步驟保持獨立乾淨卡片。停止命令沿用同一張命令卡，改用深紅，沒有錯誤圖示或「重試」語意。

登記的「登記」放大，事實文字降為中等字級與字重；「雄壯！威武！」只做一次強調。週報採紙色底，先讀到服從率，再讀口令／罰則數，最後是全文與角色。

## 色彩 token

以下為實作值。`--olive`／`--olive2` 保留原名稱作語意色；角色卡背景另由新增的 `--officer-green`／`--officer-deep` 控制。

| CSS 變數 | 淺色 | 深色 | 用途 |
|---|---|---|---|
| `--khaki` | #E3DFD0 | #171813 | 頁面背景 |
| `--ink` | #1B1D1A | #F1EFE5 | 主要文字 |
| `--olive` | #4A5733 | #7E9060 | 步驟前綴等語意色 |
| `--olive2` | #39452A | #A4B782 | 保留的第二綠色 token |
| `--signal` | #B3261E | #A92F28 | 停止命令、罰則 |
| `--mute` | #636653 | #B0B29D | 次要文字 |
| `--line` | #BCBBA8 | #535744 | 卡片與內容分隔 |
| `--card` | #F5F2E8 | #24271F | 對話框、選項、週報 |
| `--white` | #FFFFFF | #F8F5EB | 保留白色 token |
| `--memeText` | #FFFDF4 | #FFFDF4 | 深底卡片文字 |
| `--gold`（新增） | #D4AF37 | #D4AF37 | 軍階、完成大字、圖示 |
| `--paper`（新增） | #FFFFFF | #2D3027 | 步驟資訊卡 |
| `--action`（新增） | #39452A | #D4AF37 | 唯一主動作 |
| `--on-action`（新增） | #FFFDF4 | #1B1D1A | 主動作文字 |
| `--rule`（新增） | #FFFDF44D | #FFFDF44D | 深底卡片細分隔線 |
| `--shadow`（新增） | 0 4px 0 #1B1D1A16 | 0 4px 0 #0005 | 短而實的落地陰影 |

高對比模式：次要文字及一般邊線改用主文字色；選項、步驟與次要按鈕邊框加厚。深底卡片使用明亮的文字／邊線，登記強調字也改用卡片文字色。

## 三段火力

| 班長 | 主卡淺色／深色 | 登記卡淺色／深色 | 名牌與輪廓 |
|---|---|---|---|
| 阿良 · 菜鳥 | #68764B / #48583A | #50603D / #3A4830 | 26px 圓角、1px 邊框、膠囊名牌、750 字重 |
| 黑面 · 值星 | #4A5733 / #39452A | #39452A / #2B3522 | 20px 圓角、1px 邊框、雙槓軍階、850 字重 |
| 老郭 · 士官長 | #303B27 / #242E1F | #202B1D / #1C2519 | 10px 圓角、3px 邊框、實底框名牌、950 字重 |

對應新增變數：`--officer-green`、`--officer-deep`、`--radius`、`--card-edge`、`--meme-weight`。主命令大字固定 900，角色台詞依火力改字重。紅色代表 stop／罰則，不能因換班長而失去此狀態色；在紅卡保留不同輪廓及字重。

## 字級、間距、圓角

以系統字體為主：`-apple-system, PingFang TC, Noto Sans TC, system-ui`；沒有新增字型下載或授權依賴。`html` 沿用 `-apple-system-body` 支援；以下 rem 值依系統基準變化，不限制使用者縮放。

| 層級 | 實作值 | 字重／行高 |
|---|---|---|
| 班長姓名 | 1.8rem | 850 / 1.2 |
| 首頁主要「吃」 | 5rem | 850 / 1 |
| 首頁其餘主字 | 2.7rem | 850 / 1.05 |
| 對話框 | 1.2rem | 750 / 1.45 |
| 命令上段 | clamp(1rem, .8rem + .85vw, 1.2rem) | 火力字重 / 1.28 |
| 命令本體 | clamp(1.9rem, 1.15rem + 5vw, 3rem) | 900 / 1.16 |
| 命令下段 | clamp(.9rem, .8rem + .45vw, 1.08rem) | 550 / 1.5 |
| 登記事實 | clamp(1.3rem, 1rem + 1.9vw, 2rem) | 600 / 1.5 |
| 倒數 | clamp(4rem, 2rem + 15vw, 7rem) | 850 / 1，等寬數字 |
| 週報主數字 | clamp(4rem, 3rem + 9vw, 6rem) | 800 / 1 |
| 週報正文 | clamp(1rem, .95rem + .2vw, 1.125rem) | 400 / 1.65 |
| 操作按鈕 | 1rem | 750；最小高 56px |
| 步驟正文 | .9375rem | 400 / 1.65 |

間距階梯：4 / 8 / 12 / 16 / 20 / 24 / 40 / 48px。頁面左右 20px，最大內容寬 440px；一般畫面區塊間距 16px，首頁 12px；磁貼間距 10px；chips 間距 8px。通用按鈕及步驟圓角 16px、chips 12px、次要膠囊 999px，角色主卡依上表。

## 各畫面尺寸與內容行為

| 畫面 | 固定／最小尺寸 | 隨內容變化 | clamp 與注意事項 |
|---|---|---|---|
| 01 首頁 | 頭像 96×104；主磁貼至少 150 高；其他至少 128 高 | 對話框、副標及磁貼可增高 | 班長最先出現；日記連結退居其下；高 <700 時三小鍵單列橫捲 |
| 02 點名 | 五顆星各至少 44×56；chips 至少 48 高；主鍵至少 56 高 | 選項換行，表單向下延伸 | 選中 chips 有填色、字重與勾號；星數有 aria-pressed；一顆「是！班長」 |
| 03 命令 | 390 寬下卡片 350×437.5；左右約 52%／48%；內距 20 | 長店名以 anywhere 換行；步驟與下方操作隨內容排 | 上／大／下皆 clamp；右欄角色不使用絕對定位；主鍵一整列，次要操作在下一列 |
| 04 停止 | 同命令卡 | 同上 | 只以 stop 語意上紅色；不增加錯誤文案 |
| 05 登記 | 同 4:5 卡；主鍵至少 56 高 | 事實文字自然換行，保留完整登記句 | 登記大標與事實分開；完成強調一次；分享鍵次要 |
| 06 罰則 | 同 4:5 卡；步數沿用 10／20／30 | 下段文字可換行；底部節拍格依總次數等分 | 倒數 clamp、等寬數字；0 顯示 420ms 後自動解散，不新增操作 |
| 07 週報 | 頂部綠線 6px；角色 72×72；左右內距 22 | 卡片單欄、自動高度；完整保留段落 | 服從率唯一 hero；口令／罰則退後；全文与主數字／標籤皆 clamp |
| 08 日記空狀態 | 三格統計；紙張線條圖示 58×68 | 空狀態正文自動換行，主鍵在頁底 | 頭像＋班長姓名仍在最上；沿用原文與原週報入口 |
| 09 啟動 | 圖示 96×96，畫面幾何置中 | 背景延伸滿版 | 無文字、無動畫；淺色 #E3DFD0、深色 #171813 |

所有畫面保留頂部與底部 `env(safe-area-inset-*)`。本次 PNG 是 390×844 的內容視窗，不添加假的 iOS 狀態列或瀏海。包成 Capacitor 後需驗證實機 safe area 與系統動態字級。

### 大字級與 4:5 的處理

標準閱讀卡片維持 4:5。當系統超大字級使長文字超過該高度，閱讀卡片允許向下延伸，完整顯示文字；不壓字、不裁切，也不把使用者指定的大字縮回去。此為本版已告知的設計處理，與「所有字級下閱讀卡片都固定 4:5」的嚴格解讀有差異；若仍要求該解讀，這一條需另行決定，不能寫成無條件通過。

分享採隔離版型：命令／登記固定 1080×1350，週報則 1080 寬、依正文長度自動增高。使用者正在閱讀的卡片不會因輸出而改尺寸。角色配色以計算後的 token 複製到輸出容器，生成後移除容器。`share-long-test.png` 為 32px 系統基準字級下，15 字店名及長上下段的實際輸出證據。

## 動態、觸覺與聲音規格

| 事件 | 已實作的視覺 | Capacitor 觸覺規格（尚待串接） |
|---|---|---|
| 命令進場 | 180ms，向上 6px，同時淡入，ease-out、無彈跳 | 確認下令時一次輕觸；避免按下與卡片出現各震一次 |
| 按主鍵 | 120ms，向下 2px，短陰影收起 | 完成登記一次中等觸覺 |
| 倒數 | 160ms，由 0.95 到 1；底部節拍格減少 | 每一個計數事件一次短觸覺；依現有 1000/600/500ms 節拍 |
| 歸零 | 紅色轉回角色綠色、表情 idle，保留 420ms | 一次長觸覺；不可同時重複短觸覺 |
| 完成 | 「雄壯！威武！」240ms、0.96 到 1，只一次 | 與完成登記使用同一次中等觸覺 |
| 換班長 | 頭像重繪，主色／邊框／圓角 180ms 同步變化 | 一次輕觸 |

`prefers-reduced-motion:reduce` 移除全部動畫與 transition；倒數的真實時間與數字照常運作。原生觸覺也應尊重系統與使用者偏好，減少動態時不播放逐拍裝飾觸覺。離開畫面時解除計時器，避免背景繼續震動。

聲音不列入此版必需資產，預設靜音。可選規格為下令單聲短木塊音、完成單聲收束；如日後加入，必須尊重靜音模式，不自動播放語音或軍樂。本版沒有加入聲音或新設定頁。

## 工程落點

- `src/styles.css`：完整 token、三段火力、深淺色、版面、動態與分享版型。
- `src/app.tsx`：班長順序／頭像按鍵可及性、星數狀態、週報數字、倒數歸零釋放。
- `src/meme.tsx`：保持兩欄；加入 mood 與倒數節拍標記。
- `src/share.ts`：隔離分享輸出，固定尺寸，完成／錯誤均清理容器。
- `design/`、`design.html`、`vite.design.config.ts`：獨立展示入口；固定展示資料不進正式 `index.html`。

沒有更改 AvatarProps、正式角色資產路徑、API 規格、資料 schema 或登入流程。原生圖示與啟動畫面尚未加入 Xcode asset catalog；此 repo 尚無 Capacitor 原生專案，交付的是可導入資產與規格。

## 驗證邊界

正式 build 與 84 個單元測試通過。內建瀏覽器完成畫面與指定互動檢查。`npm run e2e:shots` 在 WebKit 啟動時遭 macOS sandbox 權限阻擋，未執行到網頁；沒有宣稱 Playwright E2E 或 iOS 真機驗收通過。此為可審閱的 UI 實作／設計提案，不是 App Store 上架完成。


## 8-bit 角色更新（2026-09-13）

依新簡報切換為原生 48×48、三人各五表情的透明像素資產。使用既有路徑自動載入，沒有修改 AvatarProps 或 Mood 型別。四種既有狀態已接入；soft 提供檔案，由產品端後續接入。三人共用膚色，制服由亮到暗；老郭紅哨繩補入第八色，已獲使用者確認。

所有 img 使用 object-fit:contain 與 image-rendering:pixelated。命令卡不再用圓形遮罩裁切透明角色，白名牌、值星帶與肩線完整顯示。角色仍在獨立右欄，不壓文字。

圖示也改成金環像素徽章，1024×1024、完全不透明、無文字、無五角星；iOS 圓角交由系統套用。

完整角色檔、製作方式與值星帶照片查證見 characters/README.md。真人五名退伍者的文化辨識盲測尚待執行，不能標成通過。


## 日式 8-bit 方向更新（2026-09-14）

新版角色增加小耳朵、縮短眉毛、修整眼睛與翻領，保留48×48與原8色色表。使用內建imagegen製作日式Famicom肖像方向圖（retro-direction/character-direction.png），正式15張仍以格線精修產出，避免大圖直接縮小失真。介面改用2px方角、硬邊陰影、虛線分隔及方格節拍；三段火力保留綠色明暗與字重差異。繁中文字維持系統字。

本次build與型別檢查通過；尚未重出七畫面截圖或完成新版互動視覺驗收。screens/保留並標示為上版，實際新版見play.html。
